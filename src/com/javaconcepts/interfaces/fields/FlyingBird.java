package com.javaconcepts.interfaces.fields;

// Both the Bird and the FlyingBird interface variables are treated as equal when compiled
public interface FlyingBird {
    public static final int MAX_HEIGHT_IN_FEET = 2000;
}
