package com.javaconcepts.interfaces.benefits.abstraction;

// Using Interface, we can achieve full abstraction
// We can define WHAT class must do
// but not HOW it will do
public interface Bird {
    public void fly();
}
