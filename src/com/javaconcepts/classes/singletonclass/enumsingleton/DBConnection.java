package com.javaconcepts.classes.singletonclass.enumsingleton;

// 6. Enum Singleton: The enum itself represents the single instance
// It is short, safe, and prevents multiple instances naturally

public enum DBConnection {
    INSTANCE;
}
